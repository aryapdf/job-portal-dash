"use client"

export default function Page() {
  const style: any = {
    container: {
      width: "100vw",
      flex: "1 1 auto",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center",
    },
  };

  return (
    <div
        style={{
          ...style.container,
      }}
    >
      <div
        className="w-full relative"
        style={{

        }}
      >

      </div>
    </div>
  );
}
